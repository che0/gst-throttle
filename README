This package contains:

 - throttle filter
	that can throttle pipeline to real time or print info abount what comes through

 - splitter
	that divides pipeline stream with both H.264 and AAC buffers to two output pads

It's all badly designed, experimental, etc.
